{
  "name": "wraps-mvp",
  "private": true,
  "workspaces": [
    "server",
    "web"
  ],
  "scripts": {
    "dev": "npm -w server run dev",
    "dev:all": "concurrently \"npm -w server run dev\" \"npm -w web run dev\"",
    "build": "cd web && npm run build && cd ../server && npm run build",
    "start": "cd server && npm run start"
  },
  "devDependencies": {
    "@flydotio/dockerfile": "^0.7.10",
    "concurrently": "^9.0.0"
  }
}
